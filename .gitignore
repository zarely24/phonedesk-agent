# Root node_modules only - resources/ws-scrcpy/node_modules MUST stay committed
# (the shipped ws-scrcpy needs its runtime deps).
/node_modules/
/dist-installers/
*.log
.check-asar.js
# dev leftovers - .agent.json/.debug-agent.json hold device tokens, never commit
.agent.json
.debug-agent.json
debug-*.js
probe-stream.js
