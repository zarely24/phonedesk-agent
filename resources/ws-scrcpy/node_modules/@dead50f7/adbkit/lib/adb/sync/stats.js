"use strict";
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var Fs = __importStar(require("fs"));
var Stats = /** @class */ (function (_super) {
    __extends(Stats, _super);
    function Stats(mode, size, mtime) {
        var _this = _super.call(this) || this;
        _this.mode = mode;
        _this.size = size;
        _this.mtime = new Date(mtime * 1000);
        return _this;
    }
    // The following constant were extracted from `man 2 stat` on Ubuntu 12.10.
    Stats.S_IFMT = 61440; // bit mask for the file type bit fields
    Stats.S_IFSOCK = 49152; // socket
    Stats.S_IFLNK = 40960; // symbolic link
    Stats.S_IFREG = 32768; // regular file
    Stats.S_IFBLK = 24576; // block device
    Stats.S_IFDIR = 16384; // directory
    Stats.S_IFCHR = 8192; // character device
    Stats.S_IFIFO = 4096; // FIFO
    Stats.S_ISUID = 2048; // set UID bit
    Stats.S_ISGID = 1024; // set-group-ID bit (see below)
    Stats.S_ISVTX = 512; // sticky bit (see below)
    Stats.S_IRWXU = 448; // mask for file owner permissions
    Stats.S_IRUSR = 256; // owner has read permission
    Stats.S_IWUSR = 128; // owner has write permission
    Stats.S_IXUSR = 64; // owner has execute permission
    Stats.S_IRWXG = 56; // mask for group permissions
    Stats.S_IRGRP = 32; // group has read permission
    return Stats;
}(Fs.Stats));
module.exports = Stats;
