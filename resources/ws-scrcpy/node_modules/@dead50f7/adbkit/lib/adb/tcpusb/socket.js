"use strict";
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
var events_1 = require("events");
var crypto = __importStar(require("crypto"));
var debug_1 = __importDefault(require("debug"));
var bluebird_1 = __importDefault(require("bluebird"));
var packetreader_1 = __importDefault(require("./packetreader"));
var rollingcounter_1 = __importDefault(require("./rollingcounter"));
var packet_1 = __importDefault(require("./packet"));
var auth_1 = __importDefault(require("../auth"));
var servicemap_1 = __importDefault(require("./servicemap"));
var service_1 = __importDefault(require("./service"));
var debug = (0, debug_1.default)('adb:tcpusb:socket');
var UINT32_MAX = 0xffffffff;
var UINT16_MAX = 0xffff;
var AUTH_TOKEN = 1;
var AUTH_SIGNATURE = 2;
var AUTH_RSAPUBLICKEY = 3;
var TOKEN_LENGTH = 20;
var AuthError = /** @class */ (function (_super) {
    __extends(AuthError, _super);
    function AuthError(message) {
        var _this = _super.call(this, message) || this;
        Object.setPrototypeOf(_this, AuthError.prototype);
        _this.name = 'AuthError';
        Error.captureStackTrace(_this, Socket.AuthError);
        return _this;
    }
    return AuthError;
}(Error));
var UnauthorizedError = /** @class */ (function (_super) {
    __extends(UnauthorizedError, _super);
    function UnauthorizedError() {
        var _this = _super.call(this, 'Unauthorized access') || this;
        Object.setPrototypeOf(_this, UnauthorizedError.prototype);
        _this.name = 'UnauthorizedError';
        Error.captureStackTrace(_this, Socket.UnauthorizedError);
        return _this;
    }
    return UnauthorizedError;
}(Error));
var Socket = /** @class */ (function (_super) {
    __extends(Socket, _super);
    function Socket(client, serial, socket, options) {
        if (options === void 0) { options = {}; }
        var _this = _super.call(this) || this;
        _this.client = client;
        _this.serial = serial;
        _this.socket = socket;
        _this.options = options;
        _this.ended = false;
        _this.authorized = false;
        _this.syncToken = new rollingcounter_1.default(UINT32_MAX);
        _this.remoteId = new rollingcounter_1.default(UINT32_MAX);
        _this.services = new servicemap_1.default();
        _this.version = 1;
        _this.maxPayload = 4096;
        var base;
        (base = _this.options).auth || (base.auth = function () { return bluebird_1.default.resolve(true); });
        _this.socket.setNoDelay(true);
        _this.reader = new packetreader_1.default(_this.socket)
            .on('packet', _this._handle.bind(_this))
            .on('error', function (err) {
            debug("PacketReader error: " + err.message);
            return _this.end();
        })
            .on('end', _this.end.bind(_this));
        _this.remoteAddress = _this.socket.remoteAddress;
        _this.token = null;
        _this.signature = null;
        return _this;
    }
    Socket.prototype.end = function () {
        if (this.ended) {
            return this;
        }
        // End services first so that they can send a final payload before FIN.
        this.services.end();
        this.socket.end();
        this.ended = true;
        this.emit('end');
        return this;
    };
    Socket.prototype._error = function (err) {
        this.emit('error', err);
        return this.end();
    };
    Socket.prototype._handle = function (packet) {
        var _this = this;
        if (this.ended) {
            return;
        }
        this.emit('userActivity', packet);
        return bluebird_1.default.try(function () {
            switch (packet.command) {
                case packet_1.default.A_SYNC:
                    return _this._handleSyncPacket();
                case packet_1.default.A_CNXN:
                    return _this._handleConnectionPacket(packet);
                case packet_1.default.A_OPEN:
                    return _this._handleOpenPacket(packet);
                case packet_1.default.A_OKAY:
                case packet_1.default.A_WRTE:
                case packet_1.default.A_CLSE:
                    return _this._forwardServicePacket(packet);
                case packet_1.default.A_AUTH:
                    return _this._handleAuthPacket(packet);
                default:
                    throw new Error("Unknown command " + packet.command);
            }
        })
            .catch(Socket.AuthError, function () {
            return _this.end();
        })
            .catch(Socket.UnauthorizedError, function () {
            return _this.end();
        })
            .catch(function (err) {
            return _this._error(err);
        });
    };
    Socket.prototype._handleSyncPacket = function () {
        // No need to do anything?
        debug('I:A_SYNC');
        debug('O:A_SYNC');
        return this.write(packet_1.default.assemble(packet_1.default.A_SYNC, 1, this.syncToken.next(), null));
    };
    Socket.prototype._handleConnectionPacket = function (packet) {
        var _this = this;
        debug('I:A_CNXN', packet);
        this.version = packet_1.default.swap32(packet.arg0);
        this.maxPayload = Math.min(UINT16_MAX, packet.arg1);
        return this._createToken().then(function (token) {
            _this.token = token;
            debug("Created challenge '" + _this.token.toString('base64') + "'");
            debug('O:A_AUTH');
            return _this.write(packet_1.default.assemble(packet_1.default.A_AUTH, AUTH_TOKEN, 0, _this.token));
        });
    };
    Socket.prototype._handleAuthPacket = function (packet) {
        var _this = this;
        debug('I:A_AUTH', packet);
        switch (packet.arg0) {
            case AUTH_SIGNATURE:
                // Store first signature, ignore the rest
                debug("Received signature '" + packet.data.toString('base64') + "'");
                if (!this.signature) {
                    this.signature = packet.data;
                }
                debug('O:A_AUTH');
                return this.write(packet_1.default.assemble(packet_1.default.A_AUTH, AUTH_TOKEN, 0, this.token));
            case AUTH_RSAPUBLICKEY:
                if (!this.signature) {
                    throw new Socket.AuthError('Public key sent before signature');
                }
                if (!packet.data || packet.data.length < 2) {
                    throw new Socket.AuthError('Empty RSA public key');
                }
                debug("Received RSA public key '" + packet.data.toString('base64') + "'");
                return auth_1.default.parsePublicKey(this._skipNull(packet.data).toString())
                    .then(function (key) {
                    var digest = _this.token.toString('binary');
                    var sig = _this.signature.toString('binary');
                    if (!key.verify(digest, sig)) {
                        debug('Signature mismatch');
                        throw new Socket.AuthError('Signature mismatch');
                    }
                    debug('Signature verified');
                    return key;
                })
                    .then(function (key) {
                    return _this.options.auth(key).catch(function () {
                        debug('Connection rejected by user-defined auth handler');
                        throw new Socket.AuthError('Rejected by user-defined handler');
                    });
                })
                    .then(function () {
                    return _this._deviceId();
                })
                    .then(function (id) {
                    _this.authorized = true;
                    debug('O:A_CNXN');
                    return _this.write(packet_1.default.assemble(packet_1.default.A_CNXN, packet_1.default.swap32(_this.version), _this.maxPayload, id));
                });
            default:
                throw new Error("Unknown authentication method " + packet.arg0);
        }
    };
    Socket.prototype._handleOpenPacket = function (packet) {
        var _this = this;
        if (!this.authorized) {
            throw new Socket.UnauthorizedError();
        }
        var remoteId = packet.arg0;
        var localId = this.remoteId.next();
        if (!(packet.data && packet.data.length >= 2)) {
            throw new Error('Empty service name');
        }
        var name = this._skipNull(packet.data);
        debug("Calling " + name);
        var service = new service_1.default(this.client, this.serial, localId, remoteId, this);
        return new bluebird_1.default(function (resolve, reject) {
            service.on('error', reject);
            service.on('end', resolve);
            _this.services.insert(localId, service);
            debug("Handling " + _this.services.count + " services simultaneously");
            return service.handle(packet);
        })
            .catch(function () {
            return true;
        })
            .finally(function () {
            _this.services.remove(localId);
            debug("Handling " + _this.services.count + " services simultaneously");
            return service.end();
        });
    };
    Socket.prototype._forwardServicePacket = function (packet) {
        if (!this.authorized) {
            throw new Socket.UnauthorizedError();
        }
        var localId = packet.arg1;
        var service = this.services.get(localId);
        if (service) {
            return service.handle(packet);
        }
        debug('Received a packet to a service that may have been closed already');
    };
    Socket.prototype.write = function (chunk) {
        if (this.ended) {
            return;
        }
        return this.socket.write(chunk);
    };
    Socket.prototype._createToken = function () {
        return bluebird_1.default.promisify(crypto.randomBytes)(TOKEN_LENGTH);
    };
    Socket.prototype._skipNull = function (data) {
        return data.slice(0, -1); // Discard null byte at end
    };
    Socket.prototype._deviceId = function () {
        debug('Loading device properties to form a standard device ID');
        return this.client.getProperties(this.serial).then(function (properties) {
            var id = (function () {
                var ref = ['ro.product.name', 'ro.product.model', 'ro.product.device'];
                var results = [];
                for (var i = 0, len = ref.length; i < len; i++) {
                    var prop = ref[i];
                    results.push(prop + "=" + properties[prop] + ";");
                }
                return results;
            })().join('');
            return new Buffer("device::" + id + "\0");
        });
    };
    Socket.AuthError = AuthError;
    Socket.UnauthorizedError = UnauthorizedError;
    return Socket;
}(events_1.EventEmitter));
module.exports = Socket;
