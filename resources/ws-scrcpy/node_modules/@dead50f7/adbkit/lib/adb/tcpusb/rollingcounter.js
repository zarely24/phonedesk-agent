"use strict";
var RollingCounter = /** @class */ (function () {
    function RollingCounter(max, min) {
        if (min === void 0) { min = 1; }
        this.max = max;
        this.min = min;
        this.now = this.min;
    }
    RollingCounter.prototype.next = function () {
        if (!(this.now < this.max)) {
            this.now = this.min;
        }
        return ++this.now;
    };
    return RollingCounter;
}());
module.exports = RollingCounter;
