"use strict";
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
var events_1 = require("events");
var packet_1 = __importDefault(require("./packet"));
var ChecksumError = /** @class */ (function (_super) {
    __extends(ChecksumError, _super);
    function ChecksumError(packet) {
        var _this = _super.call(this) || this;
        _this.packet = packet;
        Object.setPrototypeOf(_this, ChecksumError.prototype);
        _this.name = 'ChecksumError';
        _this.message = 'Checksum mismatch';
        Error.captureStackTrace(_this, PacketReader.ChecksumError);
        return _this;
    }
    return ChecksumError;
}(Error));
var MagicError = /** @class */ (function (_super) {
    __extends(MagicError, _super);
    function MagicError(packet) {
        var _this = _super.call(this) || this;
        _this.packet = packet;
        Object.setPrototypeOf(_this, MagicError.prototype);
        _this.name = 'MagicError';
        _this.message = 'Magic value mismatch';
        Error.captureStackTrace(_this, PacketReader.MagicError);
        return _this;
    }
    return MagicError;
}(Error));
var PacketReader = /** @class */ (function (_super) {
    __extends(PacketReader, _super);
    function PacketReader(stream) {
        var _this = _super.call(this) || this;
        _this.stream = stream;
        _this.inBody = false;
        _this.buffer = null;
        _this.packet = null;
        _this.stream.on('readable', _this._tryRead.bind(_this));
        _this.stream.on('error', function (err) {
            return _this.emit('error', err);
        });
        _this.stream.on('end', function () {
            return _this.emit('end');
        });
        setImmediate(_this._tryRead.bind(_this));
        return _this;
    }
    PacketReader.prototype._tryRead = function () {
        while (this._appendChunk()) {
            while (this.buffer) {
                if (this.inBody) {
                    if (!(this.buffer.length >= this.packet.length)) {
                        break;
                    }
                    this.packet.data = this._consume(this.packet.length);
                    if (!this.packet.verifyChecksum()) {
                        this.emit('error', new PacketReader.ChecksumError(this.packet));
                        return;
                    }
                    this.emit('packet', this.packet);
                    this.inBody = false;
                }
                else {
                    if (!(this.buffer.length >= 24)) {
                        break;
                    }
                    var header = this._consume(24);
                    this.packet = new packet_1.default(header.readUInt32LE(0), header.readUInt32LE(4), header.readUInt32LE(8), header.readUInt32LE(12), header.readUInt32LE(16), header.readUInt32LE(20), new Buffer(0));
                    if (!this.packet.verifyMagic()) {
                        this.emit('error', new PacketReader.MagicError(this.packet));
                        return;
                    }
                    if (this.packet.length === 0) {
                        this.emit('packet', this.packet);
                    }
                    else {
                        this.inBody = true;
                    }
                }
            }
        }
    };
    PacketReader.prototype._appendChunk = function () {
        var chunk = this.stream.read();
        if (chunk) {
            if (this.buffer) {
                return (this.buffer = Buffer.concat([this.buffer, chunk], this.buffer.length + chunk.length));
            }
            else {
                return (this.buffer = chunk);
            }
        }
        else {
            return null;
        }
    };
    PacketReader.prototype._consume = function (length) {
        var chunk = this.buffer.slice(0, length);
        this.buffer = length === this.buffer.length ? null : this.buffer.slice(length);
        return chunk;
    };
    PacketReader.ChecksumError = ChecksumError;
    PacketReader.MagicError = MagicError;
    return PacketReader;
}(events_1.EventEmitter));
module.exports = PacketReader;
