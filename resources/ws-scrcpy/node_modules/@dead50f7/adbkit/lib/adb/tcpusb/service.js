"use strict";
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
var debug_1 = __importDefault(require("debug"));
var events_1 = require("events");
var packet_1 = __importDefault(require("./packet"));
var bluebird_1 = __importDefault(require("bluebird"));
var protocol_1 = __importDefault(require("../protocol"));
var debug = (0, debug_1.default)('adb:tcpusb:service');
var PrematurePacketError = /** @class */ (function (_super) {
    __extends(PrematurePacketError, _super);
    function PrematurePacketError(packet) {
        var _this = _super.call(this) || this;
        _this.packet = packet;
        Object.setPrototypeOf(_this, PrematurePacketError.prototype);
        _this.name = 'PrematurePacketError';
        _this.message = 'Premature packet';
        Error.captureStackTrace(_this, Service.PrematurePacketError);
        return _this;
    }
    return PrematurePacketError;
}(Error));
var LateTransportError = /** @class */ (function (_super) {
    __extends(LateTransportError, _super);
    function LateTransportError() {
        var _this = _super.call(this) || this;
        Object.setPrototypeOf(_this, LateTransportError.prototype);
        _this.name = 'LateTransportError';
        _this.message = 'Late transport';
        Error.captureStackTrace(_this, Service.LateTransportError);
        return _this;
    }
    return LateTransportError;
}(Error));
var Service = /** @class */ (function (_super) {
    __extends(Service, _super);
    function Service(client, serial, localId, remoteId, socket) {
        var _this = _super.call(this) || this;
        _this.client = client;
        _this.serial = serial;
        _this.localId = localId;
        _this.remoteId = remoteId;
        _this.socket = socket;
        _this.opened = false;
        _this.ended = false;
        _this.transport = null;
        _this.needAck = false;
        return _this;
    }
    Service.prototype.end = function () {
        if (this.transport) {
            this.transport.end();
        }
        if (this.ended) {
            return this;
        }
        debug('O:A_CLSE');
        var localId = this.opened ? this.localId : 0; // Zero can only mean a failed open
        try {
            // We may or may not have gotten here due to @socket ending, so write
            // may fail.
            this.socket.write(packet_1.default.assemble(packet_1.default.A_CLSE, localId, this.remoteId, null));
        }
        catch (error) { }
        // Let it go
        this.transport = null;
        this.ended = true;
        this.emit('end');
        return this;
    };
    Service.prototype.handle = function (packet) {
        var _this = this;
        return bluebird_1.default.try(function () {
            switch (packet.command) {
                case packet_1.default.A_OPEN:
                    return _this._handleOpenPacket(packet);
                case packet_1.default.A_OKAY:
                    return _this._handleOkayPacket(packet);
                case packet_1.default.A_WRTE:
                    return _this._handleWritePacket(packet);
                case packet_1.default.A_CLSE:
                    return _this._handleClosePacket(packet);
                default:
                    throw new Error("Unexpected packet " + packet.command);
            }
        }).catch(function (err) {
            _this.emit('error', err);
            return _this.end();
        });
    };
    Service.prototype._handleOpenPacket = function (packet) {
        var _this = this;
        debug('I:A_OPEN', packet);
        return this.client
            .transport(this.serial)
            .then(function (transport) {
            _this.transport = transport;
            if (_this.ended) {
                throw new LateTransportError();
            }
            _this.transport.write(protocol_1.default.encodeData(packet.data.slice(0, -1))); // Discard null byte at end
            return _this.transport.parser.readAscii(4).then(function (reply) {
                switch (reply) {
                    case protocol_1.default.OKAY:
                        debug('O:A_OKAY');
                        _this.socket.write(packet_1.default.assemble(packet_1.default.A_OKAY, _this.localId, _this.remoteId, null));
                        return (_this.opened = true);
                    case protocol_1.default.FAIL:
                        return _this.transport.parser.readError();
                    default:
                        return _this.transport.parser.unexpected(reply, 'OKAY or FAIL');
                }
            });
        })
            .then(function () {
            return new bluebird_1.default(function (resolve, reject) {
                _this.transport.socket
                    .on('readable', function () {
                    return _this._tryPush();
                })
                    .on('end', resolve)
                    .on('error', reject);
                return _this._tryPush();
            });
        })
            .finally(function () {
            return _this.end();
        });
    };
    Service.prototype._handleOkayPacket = function (packet) {
        debug('I:A_OKAY', packet);
        if (this.ended) {
            return;
        }
        if (!this.transport) {
            throw new Service.PrematurePacketError(packet);
        }
        this.needAck = false;
        return this._tryPush();
    };
    Service.prototype._handleWritePacket = function (packet) {
        debug('I:A_WRTE', packet);
        if (this.ended) {
            return;
        }
        if (!this.transport) {
            throw new Service.PrematurePacketError(packet);
        }
        if (packet.data) {
            this.transport.write(packet.data);
        }
        debug('O:A_OKAY');
        return this.socket.write(packet_1.default.assemble(packet_1.default.A_OKAY, this.localId, this.remoteId, null));
    };
    Service.prototype._handleClosePacket = function (packet) {
        debug('I:A_CLSE', packet);
        if (this.ended) {
            return;
        }
        if (!this.transport) {
            throw new Service.PrematurePacketError(packet);
        }
        return this.end();
    };
    Service.prototype._tryPush = function () {
        var chunk;
        if (this.needAck || this.ended) {
            return;
        }
        if ((chunk = this._readChunk(this.transport.socket))) {
            debug('O:A_WRTE');
            this.socket.write(packet_1.default.assemble(packet_1.default.A_WRTE, this.localId, this.remoteId, chunk));
            return (this.needAck = true);
        }
    };
    Service.prototype._readChunk = function (stream) {
        return stream.read(this.socket.maxPayload) || stream.read();
    };
    Service.PrematurePacketError = PrematurePacketError;
    Service.LateTransportError = LateTransportError;
    return Service;
}(events_1.EventEmitter));
module.exports = Service;
