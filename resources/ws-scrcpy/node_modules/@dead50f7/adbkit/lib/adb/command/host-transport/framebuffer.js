"use strict";
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
var child_process_1 = require("child_process");
var debug_1 = __importDefault(require("debug"));
var rgbtransform_1 = __importDefault(require("../../framebuffer/rgbtransform"));
var protocol_1 = __importDefault(require("../../protocol"));
var command_1 = __importDefault(require("../../command"));
var debug = (0, debug_1.default)('adb:command:framebuffer');
// FIXME(intentional any): not "any" will break it all
// eslint-disable-next-line @typescript-eslint/no-explicit-any
var FrameBufferCommand = /** @class */ (function (_super) {
    __extends(FrameBufferCommand, _super);
    function FrameBufferCommand() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    FrameBufferCommand.prototype.execute = function (format) {
        var _this = this;
        this._send('framebuffer:');
        return this.parser.readAscii(4).then(function (reply) {
            switch (reply) {
                case protocol_1.default.OKAY:
                    return _this.parser.readBytes(52).then(function (header) {
                        var stream;
                        var meta = _this._parseHeader(header);
                        switch (format) {
                            case 'raw':
                                stream = _this.parser.raw();
                                stream.meta = meta;
                                return stream;
                            default:
                                stream = _this._convert(meta, format);
                                stream.meta = meta;
                                return stream;
                        }
                    });
                case protocol_1.default.FAIL:
                    return _this.parser.readError();
                default:
                    return _this.parser.unexpected(reply, 'OKAY or FAIL');
            }
        });
    };
    FrameBufferCommand.prototype._convert = function (meta, format, raw) {
        debug("Converting raw framebuffer stream into " + format.toUpperCase());
        switch (meta.format) {
            case 'rgb':
            case 'rgba':
                break;
            default:
                // Known to be supported by GraphicsMagick
                debug("Silently transforming '" + meta.format + "' into 'rgb' for `gm`");
                var transform = new rgbtransform_1.default(meta);
                meta.format = 'rgb';
                raw = this.parser.raw().pipe(transform);
        }
        var proc = (0, child_process_1.spawn)('gm', ['convert', '-size', meta.width + "x" + meta.height, meta.format + ":-", format + ":-"]);
        raw.pipe(proc.stdin);
        return proc.stdout;
    };
    FrameBufferCommand.prototype._parseHeader = function (header) {
        var offset = 0;
        var version = header.readUInt32LE(offset);
        if (version === 16) {
            throw new Error('Old-style raw images are not supported');
        }
        offset += 4;
        var bpp = header.readUInt32LE(offset);
        offset += 4;
        var size = header.readUInt32LE(offset);
        offset += 4;
        var width = header.readUInt32LE(offset);
        offset += 4;
        var height = header.readUInt32LE(offset);
        offset += 4;
        var red_offset = header.readUInt32LE(offset);
        offset += 4;
        var red_length = header.readUInt32LE(offset);
        offset += 4;
        var blue_offset = header.readUInt32LE(offset);
        offset += 4;
        var blue_length = header.readUInt32LE(offset);
        offset += 4;
        var green_offset = header.readUInt32LE(offset);
        offset += 4;
        var green_length = header.readUInt32LE(offset);
        offset += 4;
        var alpha_offset = header.readUInt32LE(offset);
        offset += 4;
        var alpha_length = header.readUInt32LE(offset);
        var format = blue_offset === 0 ? 'bgr' : 'rgb';
        if (bpp === 32 || alpha_length) {
            format += 'a';
        }
        return {
            version: version,
            bpp: bpp,
            size: size,
            width: width,
            height: height,
            red_offset: red_offset,
            red_length: red_length,
            blue_offset: blue_offset,
            blue_length: blue_length,
            green_offset: green_offset,
            green_length: green_length,
            alpha_offset: alpha_offset,
            alpha_length: alpha_length,
            format: format,
        };
    };
    return FrameBufferCommand;
}(command_1.default));
module.exports = FrameBufferCommand;
