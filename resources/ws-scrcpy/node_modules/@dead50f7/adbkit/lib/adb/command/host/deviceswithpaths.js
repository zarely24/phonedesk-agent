"use strict";
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
var command_1 = __importDefault(require("../../command"));
var protocol_1 = __importDefault(require("../../protocol"));
var HostDevicesWithPathsCommand = /** @class */ (function (_super) {
    __extends(HostDevicesWithPathsCommand, _super);
    function HostDevicesWithPathsCommand() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    HostDevicesWithPathsCommand.prototype.execute = function () {
        var _this = this;
        this._send('host:devices-l');
        return this.parser.readAscii(4).then(function (reply) {
            switch (reply) {
                case protocol_1.default.OKAY:
                    return _this._readDevices();
                case protocol_1.default.FAIL:
                    return _this.parser.readError();
                default:
                    return _this.parser.unexpected(reply, 'OKAY or FAIL');
            }
        });
    };
    HostDevicesWithPathsCommand.prototype._readDevices = function () {
        var _this = this;
        return this.parser.readValue().then(function (value) {
            return _this._parseDevices(value);
        });
    };
    HostDevicesWithPathsCommand.prototype._parseDevices = function (value) {
        var _a;
        var device, i, id, len, line, model, path, product, transportId, type;
        var devices = [];
        if (!value.length) {
            return devices;
        }
        var ref = value.toString('ascii').split('\n');
        for (i = 0, len = ref.length; i < len; i++) {
            line = ref[i];
            if (line) {
                // For some reason, the columns are separated by spaces instead of tabs
                _a = line.split(/\s+/), id = _a[0], type = _a[1], path = _a[2], product = _a[3], model = _a[4], device = _a[5], transportId = _a[6];
                devices.push({
                    id: id,
                    type: type,
                    path: path,
                    product: product,
                    model: model,
                    device: device,
                    transportId: transportId,
                });
            }
        }
        return devices;
    };
    return HostDevicesWithPathsCommand;
}(command_1.default));
module.exports = HostDevicesWithPathsCommand;
