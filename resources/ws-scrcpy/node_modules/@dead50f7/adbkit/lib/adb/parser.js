"use strict";
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
var bluebird_1 = __importDefault(require("bluebird"));
var protocol_1 = __importDefault(require("./protocol"));
var FailError = /** @class */ (function (_super) {
    __extends(FailError, _super);
    function FailError(message) {
        var _this = _super.call(this, "Failure: '" + message + "'") || this;
        Object.setPrototypeOf(_this, FailError.prototype);
        _this.name = 'FailError';
        Error.captureStackTrace(_this, FailError);
        return _this;
    }
    return FailError;
}(Error));
var PrematureEOFError = /** @class */ (function (_super) {
    __extends(PrematureEOFError, _super);
    function PrematureEOFError(howManyMissing) {
        var _this = _super.call(this, "Premature end of stream, needed " + howManyMissing + " more bytes") || this;
        Object.setPrototypeOf(_this, PrematureEOFError.prototype);
        _this.name = 'PrematureEOFError';
        _this.missingBytes = howManyMissing;
        Error.captureStackTrace(_this, PrematureEOFError);
        return _this;
    }
    return PrematureEOFError;
}(Error));
var UnexpectedDataError = /** @class */ (function (_super) {
    __extends(UnexpectedDataError, _super);
    function UnexpectedDataError(unexpected, expected) {
        var _this = _super.call(this, "Unexpected '" + unexpected + "', was expecting " + expected) || this;
        _this.unexpected = unexpected;
        _this.expected = expected;
        Object.setPrototypeOf(_this, UnexpectedDataError.prototype);
        _this.name = 'UnexpectedDataError';
        Error.captureStackTrace(_this, UnexpectedDataError);
        return _this;
    }
    return UnexpectedDataError;
}(Error));
var Parser = /** @class */ (function () {
    function Parser(stream) {
        this.stream = stream;
        this.ended = false;
    }
    Parser.prototype.end = function () {
        var _this = this;
        if (this.ended) {
            return bluebird_1.default.resolve(true);
        }
        var resolver = bluebird_1.default.defer();
        var tryRead = function () {
            while (_this.stream.read()) { }
        };
        var errorListener = function (err) {
            return resolver.reject(err);
        };
        var endListener = function () {
            _this.ended = true;
            return resolver.resolve(true);
        };
        this.stream.on('readable', tryRead);
        this.stream.on('error', errorListener);
        this.stream.on('end', endListener);
        this.stream.read(0);
        this.stream.end();
        return resolver.promise.cancellable().finally(function () {
            _this.stream.removeListener('readable', tryRead);
            _this.stream.removeListener('error', errorListener);
            return _this.stream.removeListener('end', endListener);
        });
    };
    Parser.prototype.raw = function () {
        return this.stream;
    };
    Parser.prototype.readAll = function () {
        var _this = this;
        var all = new Buffer(0);
        var resolver = bluebird_1.default.defer();
        var tryRead = function () {
            var chunk;
            while ((chunk = _this.stream.read())) {
                all = Buffer.concat([all, chunk]);
            }
            if (_this.ended) {
                return resolver.resolve(all);
            }
        };
        var errorListener = function (err) {
            return resolver.reject(err);
        };
        var endListener = function () {
            _this.ended = true;
            return resolver.resolve(all);
        };
        this.stream.on('readable', tryRead);
        this.stream.on('error', errorListener);
        this.stream.on('end', endListener);
        tryRead();
        return resolver.promise.cancellable().finally(function () {
            _this.stream.removeListener('readable', tryRead);
            _this.stream.removeListener('error', errorListener);
            return _this.stream.removeListener('end', endListener);
        });
    };
    Parser.prototype.readAscii = function (howMany) {
        return this.readBytes(howMany).then(function (chunk) {
            return chunk.toString('ascii');
        });
    };
    Parser.prototype.readBytes = function (howMany) {
        var _this = this;
        var resolver = bluebird_1.default.defer();
        var tryRead = function () {
            if (howMany) {
                var chunk = _this.stream.read(howMany);
                if (chunk) {
                    // If the stream ends while still having unread bytes, the read call
                    // will ignore the limit and just return what it's got.
                    howMany -= chunk.length;
                    if (howMany === 0) {
                        return resolver.resolve(chunk);
                    }
                }
                if (_this.ended) {
                    return resolver.reject(new Parser.PrematureEOFError(howMany));
                }
            }
            else {
                return resolver.resolve(new Buffer(0));
            }
        };
        var endListener = function () {
            _this.ended = true;
            return resolver.reject(new Parser.PrematureEOFError(howMany));
        };
        var errorListener = function (err) {
            return resolver.reject(err);
        };
        this.stream.on('readable', tryRead);
        this.stream.on('error', errorListener);
        this.stream.on('end', endListener);
        tryRead();
        return resolver.promise.cancellable().finally(function () {
            _this.stream.removeListener('readable', tryRead);
            _this.stream.removeListener('error', errorListener);
            return _this.stream.removeListener('end', endListener);
        });
    };
    Parser.prototype.readByteFlow = function (howMany, targetStream) {
        var _this = this;
        var resolver = bluebird_1.default.defer();
        var tryRead = function () {
            if (howMany) {
                // Try to get the exact amount we need first. If unsuccessful, take
                // whatever is available, which will be less than the needed amount.
                // avoid chunk is undefined.
                var chunk = void 0;
                while ((chunk = _this.stream.read(howMany)) || _this.stream.read()) {
                    howMany -= chunk.length;
                    targetStream.write(chunk);
                    if (howMany === 0) {
                        return resolver.resolve();
                    }
                }
                if (_this.ended) {
                    return resolver.reject(new Parser.PrematureEOFError(howMany));
                }
            }
            else {
                return resolver.resolve();
            }
        };
        var endListener = function () {
            _this.ended = true;
            return resolver.reject(new Parser.PrematureEOFError(howMany));
        };
        var errorListener = function (err) {
            return resolver.reject(err);
        };
        this.stream.on('readable', tryRead);
        this.stream.on('error', errorListener);
        this.stream.on('end', endListener);
        tryRead();
        return resolver.promise.cancellable().finally(function () {
            _this.stream.removeListener('readable', tryRead);
            _this.stream.removeListener('error', errorListener);
            return _this.stream.removeListener('end', endListener);
        });
    };
    Parser.prototype.readError = function () {
        return this.readValue().then(function (value) {
            throw new Parser.FailError(value.toString());
        });
    };
    Parser.prototype.readValue = function () {
        var _this = this;
        return this.readAscii(4).then(function (value) {
            var length = protocol_1.default.decodeLength(value);
            return _this.readBytes(length);
        });
    };
    Parser.prototype.readUntil = function (code) {
        var _this = this;
        var skipped = new Buffer(0);
        var read = function () {
            return _this.readBytes(1).then(function (chunk) {
                if (chunk[0] === code) {
                    return skipped;
                }
                else {
                    skipped = Buffer.concat([skipped, chunk]);
                    return read();
                }
            });
        };
        return read();
    };
    Parser.prototype.searchLine = function (re) {
        var _this = this;
        return this.readLine().then(function (line) {
            var match = re.exec(line.toString());
            if (match) {
                return match;
            }
            else {
                return _this.searchLine(re);
            }
        });
    };
    Parser.prototype.readLine = function () {
        return this.readUntil(0x0a).then(function (line) {
            // '\n'
            if (line[line.length - 1] === 0x0d) {
                // '\r'
                return line.slice(0, -1);
            }
            else {
                return line;
            }
        });
    };
    Parser.prototype.unexpected = function (data, expected) {
        return bluebird_1.default.reject(new Parser.UnexpectedDataError(data, expected));
    };
    Parser.FailError = FailError;
    Parser.PrematureEOFError = PrematureEOFError;
    Parser.UnexpectedDataError = UnexpectedDataError;
    return Parser;
}());
module.exports = Parser;
