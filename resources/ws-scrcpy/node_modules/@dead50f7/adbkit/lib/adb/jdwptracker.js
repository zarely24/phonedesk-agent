"use strict";
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
var events_1 = require("events");
var bluebird_1 = __importDefault(require("bluebird"));
var parser_1 = __importDefault(require("./parser"));
var JdwpTracker = /** @class */ (function (_super) {
    __extends(JdwpTracker, _super);
    function JdwpTracker(command) {
        var _this = _super.call(this) || this;
        _this.command = command;
        _this.pids = [];
        _this.pidMap = Object.create(null);
        _this.command = command;
        _this.pids = [];
        _this.pidMap = Object.create(null);
        _this.reader = _this.read()
            .catch(parser_1.default.PrematureEOFError, function () {
            return _this.emit('end');
        })
            .catch(bluebird_1.default.CancellationError, function () {
            _this.command.connection.end();
            return _this.emit('end');
        })
            .catch(function (err) {
            _this.command.connection.end();
            _this.emit('error', err);
            return _this.emit('end');
        });
        return _this;
    }
    JdwpTracker.prototype.read = function () {
        var _this = this;
        return this.command.parser
            .readValue()
            .cancellable()
            .then(function (list) {
            var pids = list.toString().split('\n');
            var maybeEmpty = pids.pop();
            if (maybeEmpty) {
                pids.push(maybeEmpty);
            }
            return _this.update(pids);
        });
    };
    JdwpTracker.prototype.update = function (newList) {
        var i, j, len, len1, pid;
        var changeSet = {
            removed: [],
            added: [],
        };
        var newMap = Object.create(null);
        for (i = 0, len = newList.length; i < len; i++) {
            pid = newList[i];
            if (!this.pidMap[pid]) {
                changeSet.added.push(pid);
                this.emit('add', pid);
                newMap[pid] = pid;
            }
        }
        var ref = this.pids;
        for (j = 0, len1 = ref.length; j < len1; j++) {
            pid = ref[j];
            if (!newMap[pid]) {
                changeSet.removed.push(pid);
                this.emit('remove', pid);
            }
        }
        this.pids = newList;
        this.pidMap = newMap;
        this.emit('changeSet', changeSet, newList);
        return this;
    };
    JdwpTracker.prototype.end = function () {
        this.reader.cancel();
        return this;
    };
    return JdwpTracker;
}(events_1.EventEmitter));
module.exports = JdwpTracker;
