"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
var client_1 = __importDefault(require("./adb/client"));
var keycode_1 = __importDefault(require("./adb/keycode"));
var util_1 = __importDefault(require("./adb/util"));
var Adb = /** @class */ (function () {
    function Adb() {
    }
    Adb.createClient = function (options) {
        if (options === void 0) { options = {}; }
        var opts = {
            bin: options.bin,
            host: options.host || process.env.ADB_HOST,
            port: options.port,
        };
        if (!opts.port) {
            var port = parseInt(process.env.ADB_PORT, 10);
            if (!isNaN(port)) {
                opts.port = port;
            }
        }
        return new client_1.default(opts);
    };
    Adb.Keycode = keycode_1.default;
    Adb.util = util_1.default;
    return Adb;
}());
module.exports = Adb;
